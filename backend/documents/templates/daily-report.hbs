<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Daily Cash Balance Report - {{date}}</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Segoe UI', Arial, sans-serif; font-size: 12px; color: #1e293b; padding: 40px; }
    .header { background: #f8fafc; padding: 20px; margin-bottom: 30px; border-radius: 8px; border-left: 4px solid #3b82f6; }
    .header h1 { font-size: 24px; color: #1e293b; margin-bottom: 8px; }
    .header .subtitle { color: #64748b; font-size: 14px; }
    .info-box { border: 1px solid #e2e8f0; padding: 15px; margin-bottom: 20px; border-radius: 6px; background: #ffffff; }
    .info-row { display: flex; justify-content: space-between; padding: 6px 0; border-bottom: 1px dashed #e2e8f0; }
    .info-row:last-child { border-bottom: none; }
    .info-label { font-weight: 600; color: #475569; }
    .info-value { color: #1e293b; }
    .section { margin-bottom: 25px; }
    .section-title { font-size: 18px; font-weight: 700; color: #0f172a; margin-bottom: 15px; padding-bottom: 8px; border-bottom: 2px solid #3b82f6; }
    .metrics-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 15px; margin-bottom: 20px; }
    .metric-card { border: 1px solid #e2e8f0; padding: 15px; border-radius: 6px; background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%); }
    .metric-label { font-size: 11px; color: #64748b; text-transform: uppercase; font-weight: 600; margin-bottom: 8px; }
    .metric-value { font-size: 20px; font-weight: 700; color: #0f172a; }
    .metric-value.positive { color: #059669; }
    .metric-value.negative { color: #dc2626; }
    table { width: 100%; border-collapse: collapse; margin-top: 15px; }
    thead { background: #f1f5f9; }
    th { text-align: left; padding: 10px; font-weight: 700; font-size: 11px; text-transform: uppercase; color: #475569; border-bottom: 2px solid #cbd5e1; }
    td { padding: 10px; border-bottom: 1px solid #e2e8f0; font-size: 12px; }
    tr:last-child td { border-bottom: none; }
    .footer { margin-top: 40px; padding-top: 20px; border-top: 1px solid #e2e8f0; text-align: center; color: #64748b; font-size: 10px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>Daily Cash Balance Report</h1>
    <div class="subtitle">{{formatDate date 'long'}} • {{propertyName}}</div>
  </div>

  <div class="info-box">
    <div class="info-row">
      <span class="info-label">Organization:</span>
      <span class="info-value">{{orgName}}</span>
    </div>
    <div class="info-row">
      <span class="info-label">Property:</span>
      <span class="info-value">{{propertyName}}</span>
    </div>
    <div class="info-row">
      <span class="info-label">Report Date:</span>
      <span class="info-value">{{formatDate date 'long'}}</span>
    </div>
    <div class="info-row">
      <span class="info-label">Generated:</span>
      <span class="info-value">{{formatDate generatedAt 'datetime'}}</span>
    </div>
  </div>

  <div class="section">
    <div class="section-title">Financial Summary</div>
    <div class="metrics-grid">
      <div class="metric-card">
        <div class="metric-label">Opening Balance</div>
        <div class="metric-value">{{formatCurrency openingBalanceCents}}</div>
      </div>
      <div class="metric-card">
        <div class="metric-label">Total Revenue</div>
        <div class="metric-value positive">{{formatCurrency totalReceivedCents}}</div>
      </div>
      <div class="metric-card">
        <div class="metric-label">Total Expenses</div>
        <div class="metric-value negative">{{formatCurrency totalExpensesCents}}</div>
      </div>
      <div class="metric-card">
        <div class="metric-label">Closing Balance</div>
        <div class="metric-value {{#if (gte closingBalanceCents 0)}}positive{{else}}negative{{/if}}">
          {{formatCurrency closingBalanceCents}}
        </div>
      </div>
    </div>
  </div>

  {{#if transactions}}
  <div class="section">
    <div class="section-title">Transaction Details</div>
    <table>
      <thead>
        <tr>
          <th>Description</th>
          <th>Type</th>
          <th>Payment Mode</th>
          <th style="text-align: right;">Amount</th>
        </tr>
      </thead>
      <tbody>
        {{#each transactions}}
        <tr>
          <td>{{this.description}}</td>
          <td>{{capitalize this.type}}</td>
          <td>{{uppercase this.paymentMode}}</td>
          <td style="text-align: right; font-weight: 600; {{#if (eq this.type 'revenue')}}color: #059669;{{else}}color: #dc2626;{{/if}}">
            {{formatCurrency this.amountCents}}
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  {{/if}}

  <div class="footer">
    <p>Generated by Hospitality Management Platform</p>
    <p>{{formatDate generatedAt 'datetime'}} IST</p>
  </div>
</body>
</html>

